# ── Framework web ────────────────────────────────────────────────────────
fastapi==0.115.0
uvicorn[standard]==0.30.6
jinja2==3.1.4
python-multipart==0.0.9

# ── Validación de datos ──────────────────────────────────────────────────
pydantic==2.9.2
python-dotenv==1.0.1

# ── Reconocimiento facial (dlib ResNet-34, alta precisión) ──────────────
# NOTA: face_recognition depende de dlib, que requiere CMake y un compilador
# de C++ instalado en el sistema (ver README.md, sección "Instalación").
cmake==3.30.4
dlib==19.24.6
face_recognition==1.3.0
face-recognition-models==0.3.0

# ── Procesamiento de imágenes ────────────────────────────────────────────
numpy==1.26.4
Pillow==10.4.0

# ── Google APIs (Sheets + Drive) ─────────────────────────────────────────
google-api-python-client==2.145.0
google-auth==2.34.0
google-auth-httplib2==0.2.0

# ── Geolocalización (reverse geocoding) ──────────────────────────────────
geopy==2.4.1

# ── Testing ───────────────────────────────────────────────────────────────
pytest==8.3.3
httpx==0.27.2
